package byow.Core;

public class Room extends IndoorArea {
    public Room(int initx, int inity, int length, int width, IndoorArea entrance) {
        super(initx, inity, length, width, entrance);
    }
}
